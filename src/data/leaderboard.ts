import { LeaderboardEntry } from '../types';

/**
 * @deprecated Mock leaderboard removed from runtime.
 * All leaderboard rankings are now queried exclusively from Supabase PostgreSQL.
 */
export const leaderboardData: LeaderboardEntry[] = [];